#include <iostream>

using namespace std;

int main() {

    int H, T;
    while (cin >> H >> T && H) {
        
        int total = 0;

        if (H % 2 != 0 && T % 2 == 0) {
            
            if (T == 2) {
                cout << H / 2 + 1 << endl;
            } else {
                
            }
        }
    }
}